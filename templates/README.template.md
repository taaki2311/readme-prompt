<!--
  README skeleton — a scaffold, not a form. Keep it SMALL.
  Sections are tagged [core] / [optional] / [rarely]: keep only those that earn
  their place for THIS project and delete the rest — the [core] set is often enough.
  Replace every <placeholder> with verified content and remove every HTML comment
  (including this block) before saving. Full rules: PROMPT.md, Steps 2–4.
-->

# <Project Name>

<!-- [core] One sentence: what it does and why a reader should care — consistent with the manifest's description field. First line, only `#` heading. -->
<One-line description.>

<!-- [optional] Badges — only ones verifiable in the repo (PROMPT.md Step 3); delete the block if none apply. -->
<!-- [![Build](badge-url)](target) [![Version](badge-url)](target) [![License](badge-url)](LICENSE) -->

<!-- [core] Short paragraph: the problem it solves and why it exists. -->
<Intro paragraph.>

<!--
  [rarely] Table of contents — only for long READMEs.
## Table of Contents
- [Installation](#installation)
- [Usage](#usage)
- [License](#license)
-->

<!-- [optional] Only if the value isn't obvious from the intro — a few concrete, code-backed bullets. -->
## Features

- <Feature grounded in the code.>
- <Feature grounded in the code.>

<!-- [optional] Only specific runtime/version/service requirements. -->
## Prerequisites

- <e.g. Node.js >= 18, Python 3.11, a running PostgreSQL instance>

## Installation

<!-- [core] The project's real install method; copy package/command names verbatim. -->
```bash
<install command, e.g. npm install <package> | pip install <package> | go get <module>>
```

## Usage

<!-- [core] One minimal verified example. CLI: one representative invocation; point to `--help` for the rest. -->
```bash
<minimal working command>
# For all options: <command> --help
```

<!-- Library: one minimal example consistent with the public API. -->
```<language>
<minimal working code example>
```

<!-- [optional] Only what a user must set (source from .env.example / config files); link to a full reference instead of duplicating it. -->
## Configuration

| Variable | Description | Default |
|---|---|---|
| `<ENV_VAR>` | <what it controls> | <default or "required"> |

<!-- [optional, often unnecessary] Point to `--help` or linked API docs; don't restate them. -->
## Reference

See `<command> --help`, or the [API docs](./docs/).

<!--
  [rarely] Project structure — a few top-level directories at most.
## Project Structure
- `src/`   <what lives here>
- `docs/`  <what lives here>
-->

<!-- [optional] When contributions are expected — the project's real scripts; link CONTRIBUTING.md instead of duplicating it. -->
## Development

```bash
<setup command>
<test command, e.g. npm test | pytest | cargo test | go test ./...>
```

<!-- [optional] Services/apps only — link to the deploy config. -->
## Deployment

<Link to the deploy config, or the few real deploy steps.>

## License

<!-- [core, if determinable] Link to LICENSE; don't paste the license text. If unknown, leave the TODO (PROMPT.md missing-info policy). -->
<Licensed under the <SPDX id> License — see [LICENSE](./LICENSE).>
<!-- TODO: confirm the license — no LICENSE file or license metadata was found -->
